

Routing :
http://localhost:50153/
Controller : Home Action : index

http://localhost:50153/ComicBools/Detail
Path 
Controller = ComicBooks Action=Detail

Pattern = Controller/action
Controller = ComicBooks, Action =Detail 
Process for matching url call == URL ROUTING 

ContentResult()
